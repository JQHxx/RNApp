import React, {Component} from 'react'
import {Text, StyleSheet} from 'react-native'

export default class MessageListCell extends Component {
    render() {
        return (
            <Text style={{padding: 12}}>消息列表</Text>
        );
    }
}